# 储能视觉 HTML 需求文档

## 1. 文档说明

本文档用于说明 `储能视觉.html` 当前版本的页面结构、视觉规则、交互逻辑、数据计算、响应式布局和动画实现要求，供后续开发、维护和设计对齐使用。

页面为单 HTML 静态页面，核心展示区域为 `Realtime Power` 实时功率卡片，下方为数据控制面板。页面支持横版与竖版自适应，Realtime Power 主体视觉由 SVG 承载，电流动画使用 Canvas 2D 绘制，弹窗、节点选择、数据联动等交互均在当前 HTML 内完成。

## 2. 文件与依赖

| 类型 | 内容 |
| --- | --- |
| 主文件 | `储能视觉.html` |
| 图标资源 | `icon/` 文件夹内的 PNG 图标 |
| 设计参考 | `储能视觉-横版.svg`、`储能视化-竖版.svg` |
| 字体 | Poppins，页面通过 Google Fonts 在线加载 |
| 动画技术 | Canvas 2D |
| 可见文字层 | Realtime Power 主体文字为 SVG 文本；数据控制面板和弹窗为 HTML/CSS 文本 |
| 运行方式 | 可直接用浏览器打开本地 HTML 文件 |

发送给他人时，需要保持 `储能视觉.html` 与 `icon/` 文件夹的相对路径不变。若对方离线打开，Poppins 字体可能无法从 Google Fonts 加载，会回退到系统无衬线字体。

## 3. 页面模块

页面包含两个主要区域：

| 区域 | 说明 |
| --- | --- |
| Realtime Power 卡片 | 展示能源节点、中间 Inverter、路径线、电流动画、节点状态、弹窗入口 |
| 数据控制面板 | 通过滑块和数字输入控制各节点功率，用于驱动页面状态与动画 |

Realtime Power 卡片内包含：

| 模块 ID | 展示名称 | 角色 |
| --- | --- | --- |
| `L1` | Solar | 供电方 |
| `L2` | Battery | 可供电 / 可用电 |
| `L3` | Vehicle | 用电方 |
| `R1` | Grid | 可供电 / 可用电 |
| `R2` | Additional Load / Dual Power | 根据数值切换模式 |
| `R3` | KeepON Load | 用电方 |
| Center | Inverter | 中间汇流与弹窗入口 |

页面支持 3 种设备类型切换，右上角设备名称和节点显示需要随设备类型同步变化。

| 设备类型 | 显示节点 | 说明 |
| --- | --- | --- |
| Power Core | Solar、Battery、Vehicle、Grid、Additional Load、KeepON Load | 6 个节点都显示；不支持 Dual Power |
| Power Store | Battery、Grid、Additional Load / Dual Power、KeepON Load | 不显示 Solar、Vehicle |
| Power Sense | Solar、Battery、Grid、Additional Load / Dual Power、KeepON Load | 不显示 Vehicle；Dual Power 存在时 Solar 仍可作为供电方 |

## 4. 节点展示规则

每个节点包含标题、状态标签、展示功率、真实功率、图标和图标外圈。

| 元素 | 规则 |
| --- | --- |
| 标题 | 使用主色 `#16AAAF` |
| 展示功率 | 根据真实功率乘以效率计算，单位 `kW` |
| 真实功率 | 显示为 `Real: xkW` |
| 状态标签 | 如 `AC Off`、`No meter`，需要与相邻文案横向居中对齐 |
| 标题 + 箭头 icon | 作为一个整体对齐，除非明确要求拆分 |
| 节点胶囊态 | 悬停或选中时显示，不改变节点内部排版 |

硬性排版规则：

| 规则 | 要求 |
| --- | --- |
| 文案 + icon | 默认横向居中对齐 |
| 文案 + 小标签 | 默认横向居中对齐 |
| Solar + 右侧箭头 | 作为一个整体，可点击打开 Solar Array |
| Inverter + 右侧箭头 | 作为一个整体，可点击打开 Inverter Details |
| 节点内部元素 | 移动节点时应作为整体移动，避免子元素脱离 |

## 5. 数据计算规则

页面使用真实功率作为底层数据，展示功率按效率换算。

| 项 | 规则 |
| --- | --- |
| 效率 | `EFF = 0.87` |
| 展示功率 | `真实功率 * 0.87`，保留 1 位小数 |
| 真实功率 | 直接用于 `Real:` 文案，保留 1 位小数 |
| 数字输入 | 用户输入展示功率，代码反推真实功率 |
| 滑块 | 滑块值按真实功率的 10 倍保存 |

各节点真实功率范围：

| 节点 | 范围 |
| --- | --- |
| Solar | `0 ~ 21.6kW` |
| Battery | `-21.6 ~ 21.6kW` |
| Vehicle | `-21.6 ~ 0kW` |
| Grid | `-21.6 ~ 21.6kW` |
| Additional Load / Dual Power | `-21.6 ~ 21.6kW` |
| KeepON Load | `-21.6 ~ 0kW` |

Power Core 不支持 Dual Power，因此 R2 正数范围不可用，实际可操作范围为 `-21.6 ~ 0kW`。

## 6. 数据控制面板

数据控制面板用于模拟实时功率变化。

| 控件 | 行为 |
| --- | --- |
| 设备类型切换 | 切换 Power Core、Power Store、Power Sense，并同步右上角设备名、节点显示和路径逻辑 |
| 滑块 | 修改真实功率，并同步展示功率、真实功率文本、节点状态和动画 |
| 数字输入 | 输入展示功率，自动换算成真实功率并同步滑块 |
| 正负切换 | 当节点功率正负状态变化时，重新计算电流方向 |
| 归零 | 某些模式切换时，相关节点必须归零 |

不同设备类型不支持的节点，需要在数据控制面板中隐藏整行，不保留置灰行。

滑块视觉规则：

| 状态 | 表现 |
| --- | --- |
| 允许范围 | 正常可拖动 |
| 禁用范围 | 灰色半透明 |
| 当前值 | 从 0 点到当前值显示绿色进度 |
| 模式切换归零 | 滑块位置和绿色路径都必须回到 0 |

## 7. Dual Power / Additional Load 规则

`R2` 节点根据设备类型和展示功率切换名称与模式。

不同设备类型下，`Dual Power / Additional Load` 的显示情况不一样：

| 设备类型 | R2 负数 / 0 | R2 正数 | 节点显示影响 |
| --- | --- | --- | --- |
| Power Core | 显示 Additional Load | 不支持 Dual Power，正数范围禁用 | 6 个节点都显示 |
| Power Store | 显示 Additional Load | 显示 Dual Power，并显示 `(PV Surplus)` | Solar、Vehicle 不显示；竖版下 Battery 与 R2 使用左侧排版，横版下 Battery 保持左中位置 |
| Power Sense | 显示 Additional Load | 显示 Dual Power，并显示 `(PV Surplus)` | Vehicle 不显示；Solar 仍显示，且可作为正数供电方 |

Dual Power 模式要求：

| 项 | 要求 |
| --- | --- |
| 节点显示 | `R2` 显示为 `Dual Power`，并显示 `(PV Surplus)` |
| Power Store | Solar、Vehicle 不存在；竖版下 Battery 与 Dual Power 使用左侧排版，横版下 Battery 保持左中位置 |
| Power Sense | Vehicle 不存在；Solar 仍显示且可作为正数供电方 |
| 已选中节点 | 如果当前选中的节点因设备类型切换被隐藏，需要取消选中 |
| 路径 | 隐藏节点不参与路径和电流动画 |

Additional Load 模式要求：

| 项 | 要求 |
| --- | --- |
| 节点显示 | `R2` 显示为 `Additional Load` |
| Power Core | 只显示 Additional Load，不进入 Dual Power |
| Power Store | Additional Load 与 Dual Power 使用同一套排版；竖版位于下左方，横版保持当前节点位置 |
| Power Sense | 竖版下 Additional Load 位于下左方 |

## 8. 节点选择与胶囊态

节点支持悬停和点击选中。

| 交互 | 行为 |
| --- | --- |
| 鼠标悬停 | 显示胶囊背景 |
| 鼠标移出 | 未选中时恢复普通状态 |
| 点击节点 | 选中节点并显示胶囊 |
| 再次点击已选中节点 | 取消选中 |
| 选中后切换横竖版 | 胶囊状态必须保留 |
| 未参与动画节点 | 在有选中节点时，未参与当前动画的节点置灰 |
| 当前选中节点 | 即使未参与动画，也不置灰 |

胶囊规则：

| 视口 | 样式 |
| --- | --- |
| 横版 | 横向胶囊 |
| 竖版 | 竖向胶囊，根据内容包裹并留边距 |

胶囊只用于节点悬停和选中反馈。未选中时，不应出现胶囊遮挡路径线。

## 9. 弹窗交互

页面包含两个弹窗。

| 入口 | 弹窗 | 内容 |
| --- | --- | --- |
| Solar 文案或右侧箭头区域 | Solar Array | MPPT 1 / MPPT 2 / MPPT 3 |
| Inverter 文案或右侧箭头区域 | Inverter Details | L1 / L2 / L3 |

弹窗通用规则：

| 项 | 要求 |
| --- | --- |
| 背景遮罩 | 仅覆盖 Realtime Power 卡片范围 |
| 遮罩效果 | 卡片内容变暗 |
| 关闭方式 | 点击弹窗外区域关闭 |
| 防误关 | 点击弹窗内部不关闭 |
| 圆角 | 弹窗卡片圆角为 16px |

弹窗内容规则：

| 设备类型 | Solar Array | Inverter Details |
| --- | --- | --- |
| Power Core | `MPPT 1`、`MPPT 2`、`MPPT 3` | `L1`、`L2`、`L3` |
| Power Store | 不显示 Solar 节点和 Solar Array 入口 | `L1`、`L2`、`L3` |
| Power Sense | `MPPT 1`、`MPPT 2` | `L1` |

弹窗卡片高度需要跟随内容自适应，不固定最小高度。内容变少时，卡片高度自然收缩。

当前弹窗数据为静态展示：

| 字段 | 值 |
| --- | --- |
| Voltage | `373.1V` |
| Current | `1A` |

## 10. 电流动画

电流动画使用 Canvas 2D 绘制，不再使用 SVG stroke-dash 动画。

核心要求：

| 项 | 要求 |
| --- | --- |
| 运动方向 | 从供电方路径起点流向中间方块，再从中间方块流向用电方 |
| 起终点 | 与圆圈灰色边缘、中间方块灰色边缘刚好贴合，不重叠、不留空 |
| 完整性 | 电流必须从路径起点流到路径终点，尾部完全流完后才算一轮结束 |
| 循环 | 一轮结束后立即开始下一轮，无明显空档 |
| 多源汇入 | 多个供电方需要同时到达中间方块 |
| 多路流出 | 多个用电方需要同时从中间方块出发，并同时流向目标 |
| 拐角 | 拐弯处必须自然，不出现翻转、折断、闪现 |
| 发光 | 电流本身不要背景发光效果 |

电流视觉：

| 项 | 规则 |
| --- | --- |
| 线宽 | 当前配置为 `4px`，保持不变 |
| 线帽 | 圆角线帽 |
| 拖尾 | 透明度渐变，线头 100% 不透明，线尾 0% |
| 背景路径 | `#000000` 6% 不透明度，不参与动画 |
| 动画颜色 | 按供电来源或混合状态决定 |

动画颜色：

| 来源 / 状态 | 色值 |
| --- | --- |
| Solar | `#F1B727` |
| Battery | `#16AAAF` |
| Grid | `#6499EA` |
| Dual Power | `#F1B727` |
| 多供电方 / 用电侧混合 | `#BA4199` |
| Inverter 主色 | `#16AAAF` |

## 11. 图标外圈与中间方块边框动画

当节点参与电流流动时，对应图标外圈显示旋转渐变效果。

| 项 | 要求 |
| --- | --- |
| 圈底色 | 纯黑色 2% 不透明度 |
| 活动渐变 | 使用对应电流颜色 |
| 渐变透明度 | `50% -> 15% -> 0%` |
| 旋转 | 参与动画时缓慢旋转 |
| 差异化 | 6 个圈旋转速度和起始角度不能完全一致 |
| 描边宽度 | 保持当前宽度，不按 Figma 6px 调整 |

中间 Inverter 方块：

| 项 | 要求 |
| --- | --- |
| 默认边框 | 与图标圈底色一致 |
| 有电流时 | 边框颜色渐变旋转 |
| 颜色 | 使用 `#16AAAF` 的透明度渐变 |
| 内容 | 图标、文字、箭头排版需与横版一致 |

## 12. 路径线规则

路径线用于表示节点与中间方块之间的连接关系。

| 项 | 要求 |
| --- | --- |
| 静态路径 | `#000000` 6% 不透明度，不使用渐变 |
| 路径端点 | 必须贴合图标灰色圆圈边缘和中间方块边缘 |
| 横版路径 | 左右两侧连接中间方块左右边 |
| 竖版普通模式 | 上下节点根据排版连接中间方块 |
| 竖版 Dual Power | 使用当前竖版普通路径规则，不单独切换为左右边连接 |
| 选中胶囊 | 可遮挡静态路径，但不能遮挡电流动画 |

## 13. 响应式布局

页面以 `753px` 为横竖版断点。

### 13.1 横版布局

| 项 | 规则 |
| --- | --- |
| 生效范围 | `>= 753px` |
| Realtime Power 最小宽度 | `753px` |
| Realtime Power 最大宽度 | `1920px` |
| 卡片高度 | `390px` |
| 浏览器边距 | 左右 20px |
| 节点布局 | 左 3 个、右 3 个，中间为 Inverter |
| 自适应方式 | 只能移动节点位置，节点内部排版和胶囊不变 |
| 路径变化 | 根据宽度拉长或缩短，中间竖线位于节点与方块之间约 1/3 位置 |

横版中，6 个节点内容排版不随宽度变化，只允许整体移动位置。路径线和 Canvas 动画需要实时跟随宽度变化，即动画进行中缩放窗口，也必须立即适配当前尺寸。

横版设备类型差异：

| 设备类型 | 排版要求 |
| --- | --- |
| Power Core | 使用标准横版 6 节点排版 |
| Power Store | 不显示 Solar、Vehicle，剩余节点位置保持稳定 |
| Power Sense | Battery 位于左下方，不使用左中位置 |

### 13.2 竖版布局

| 项 | 规则 |
| --- | --- |
| 生效范围 | `< 753px` |
| 最小宽度 | `375px` |
| 最大宽度 | `752px` |
| 浏览器边距 | 左右 20px |
| 卡片高度 | 解除横版 390px 限制，当前约 635px |
| 自适应方式 | 保持竖版排版，仅横向自适应 |

普通竖版布局：

| 位置 | 节点 |
| --- | --- |
| 上左 | Solar |
| 上中 | Battery |
| 上右 | Grid |
| 中间 | Inverter |
| 下左 | Vehicle |
| 下中 | Additional Load |
| 下右 | KeepON Load |

Power Store 竖版布局：

| 位置 | 节点 |
| --- | --- |
| 上左 | Battery |
| 中间 | Inverter |
| 下左 | Additional Load / Dual Power |
| 下右 | KeepON Load |

Power Store 中，Additional Load 和 Dual Power 使用同一套排版位置，不能因为 R2 正负切换导致位置跳变。

Power Sense 竖版布局：

| 位置 | 节点 |
| --- | --- |
| 上左 | Solar |
| 上中 | Battery |
| 上右 | Grid |
| 中间 | Inverter |
| 下左 | Additional Load / Dual Power |
| 下右 | KeepON Load |

Power Sense 中，Vehicle 不显示；Dual Power 存在时 Solar 仍保留显示和供电能力。

竖版要求：

| 项 | 要求 |
| --- | --- |
| 文案 | 居中排版 |
| 标识 | 与对应文案横向居中 |
| Additional Load | 在竖版中可拆为两行 |
| `(PV Surplus)` | 必须跟随 Dual Power 作为整体移动 |
| 胶囊 | 竖向胶囊，包裹内容并留边距 |
| 横竖切换 | 选中态、胶囊态、动画状态应正确恢复 |

## 14. 置灰规则

| 场景 | 规则 |
| --- | --- |
| 未选中节点 | 正常显示 |
| 选中某节点后 | 未参与当前电流动画的节点置灰 |
| 选中节点未参与动画 | 选中节点不置灰 |
| Solar 小卡片置灰 | Solar 右侧箭头也需要一起置灰 |
| 置灰方式 | 使用整体不透明度变化，不是单独改颜色 |

## 15. 字体与主要颜色

### 15.1 设计 token 使用规则

代码中的视觉值分为两类：

| 类型 | 规则 |
| --- | --- |
| 已绑样式 | 使用 CSS 变量表示，变量名对应 Figma 样式名或语义名；后续只改变量值即可全局生效 |
| 直接值 | 保留在具体样式或绘制逻辑中，不抽成全局 token，避免误影响其他模块 |

当前已抽离的主要 token：

| Token | 对应规范 | 当前值 |
| --- | --- | --- |
| `--color-black-01` | Black/01 | `rgba(0,0,0,.85)` |
| `--color-title-85` | Character/Title .85 | `rgba(0,0,0,.85)` |
| `--color-black-02` | Black/02 | `rgba(0,0,0,.45)` |
| `--color-black-03` | Black/03 | `rgba(0,0,0,.25)` |
| `--color-black-05` | Black/05 | `rgba(0,0,0,.06)` |
| `--color-brand-06` | Brand/06 | `#16AAAF` |
| `--color-danger-04` | Danger/04 | `#F195A7` |
| `--color-danger-01` | Danger/01 | `#FDEEF1` |
| `--color-white-01` | white/01 | `#FFFFFF` |
| `--color-neutral-13` | Neutral/13 | `#000000` |

保留直接值的内容：

| 项 | 说明 |
| --- | --- |
| 电流颜色 | 按来源直接定义，避免被通用文字 / 品牌样式误影响 |
| 圆环渐变颜色 | 按设备类型直接定义 |
| Inverter 渐变边框 | 保持当前动画规则，只调整渐变色值 |
| 弹窗遮罩 | 保持当前黑色 22% 蒙层 |
| 设备类型切换按钮 | 保持当前视觉样式 |
| hover / 选中胶囊阴影 | 保持当前视觉样式 |
| 圆环描边宽度 | 保持当前宽度 |
| 电流线宽 | 保持当前 4px |

### 15.2 字体规则

字体：

| 用途 | 字体 / 字号 / 字重 |
| --- | --- |
| 页面默认 | Poppins |
| 顶部标题 `Realtime Power` | 16px / Medium 500 |
| 主要功率数值 | 16px / Medium 500 |
| 设备名称 | 14px / Regular 400 |
| 弹窗标题 | 14px / Medium 500 |
| 弹窗分组标题 | 12px / Regular 400 |
| 弹窗辅助信息 Voltage / Current | 10px / Regular 400 |
| 状态标签 `No meter` / `AC Off` | 10px / Regular 400 |
| `Real:` 辅助数值 | 12px / Regular 400 |

Realtime Power 主体可见文字仍在 SVG 内渲染，包括标题、节点名称、功率数值、状态标签和右上角设备名称；数据控制面板和弹窗使用 HTML/CSS 文本。

历史原因说明：Realtime Power 主体原本绘制在 SVG 中，SVG 使用 `viewBox="0 0 1055 546.61"`，横版最小显示宽度为 `753px`。SVG 内部字号会随整体缩放，因此不能直接把 Figma 的像素字号写进 SVG 属性。

当前换算规则：

| Figma 视觉字号 | SVG 内部字号 token |
| --- | --- |
| 16px | `--svg-font-h5-medium: 22.43px` |
| 14px | `--svg-font-body-regular: 19.62px` |
| 12px | `--svg-font-footnote-description: 16.82px` |
| 10px | `--svg-font-footnote-small: 14.02px` |

换算后，SVG 缩放到页面上的实际视觉大小才接近 Figma 标注的字号。

### 15.3 主要颜色

主要颜色：

| 用途 | 色值 |
| --- | --- |
| 主色 / Inverter / 标题 | `#16AAAF` |
| Solar / Dual Power 电流 | `#F1B727` |
| Battery 电流 | `#16AAAF` |
| Grid 电流 | `#6499EA` |
| 用电侧混合紫色 | `#BA4199` |
| 深色文字 | `rgba(0, 0, 0, 0.85)` |
| 次级文字 | `rgba(0, 0, 0, 0.25)` |
| 静态路径 | `rgba(0, 0, 0, 0.06)` |
| 标签文字 | `#F195A7` |
| 标签背景 | `#FDEEF1` |
| 页面背景 | `#F5F5F5` |
| 卡片背景 | `#FFFFFF` |
| 圆圈底色 | `rgba(0, 0, 0, 0.02)` |

保留当前实现的视觉项：

| 项 | 规则 |
| --- | --- |
| 页面尺寸 | 保持当前横版 / 竖版尺寸和断点，不按 Figma 757 × 380px 调整 |
| 弹窗遮罩 | 保持当前黑色 22% 蒙层 |
| 设备类型切换按钮 | 保持当前样式 |
| hover / 选中胶囊阴影 | 保持当前样式 |
| 圆环描边宽度 | 保持当前宽度 |
| 电流线宽 | 保持当前 4px |
| Solar / Inverter 箭头 | 保持品牌色 `#16AAAF` |

## 16. 验收清单

开发完成或修改后，需要重点检查以下内容：

| 项 | 验收标准 |
| --- | --- |
| 本地打开 | HTML 能直接打开，图标资源正常显示 |
| 数据联动 | 滑块、输入框、节点功率和真实功率同步 |
| 设备类型 | Power Core、Power Store、Power Sense 的节点显示、右上角名称、R2 规则和弹窗分组正确 |
| 模式切换 | Additional Load 与 Dual Power 按设备类型切换正确，不支持的节点不显示且不参与动画 |
| 弹窗 | Solar 和 Inverter 的文案及箭头均可打开对应弹窗 |
| 节点选中 | 悬停、选中、取消选中、置灰状态正确 |
| 横版布局 | `753px` 以上排版稳定，最大宽度不超过 `1920px` |
| 竖版布局 | `375px ~ 752px` 排版稳定，节点整体不脱离 |
| 横竖切换 | 选中胶囊、Inverter 边框动画、箭头位置不丢失 |
| 路径端点 | 路径和电流端点贴合圆圈 / 方块边缘 |
| 电流动画 | 不闪现、不翻转、不提前消失、无明显空档 |
| 旋转圈 | 参与动画节点显示对应颜色旋转圈，底层保留 2% 黑色圈 |
| 代码清理 | 不保留废弃动画实现或无用样式 |

## 17. 后续扩展建议

| 方向 | 建议 |
| --- | --- |
| 数据接入 | 将当前 `moduleData` 替换为接口数据源，保留现有计算和动画入口 |
| 设计规范 | 后续可将字体、字号、字重、颜色抽离为 CSS 变量 |
| 组件化 | 若迁移到前端框架，可拆为 RealtimePower、PowerNode、FlowCanvas、ControlPanel、DetailModal |
| 动画维护 | 保持 Canvas 2D 作为电流动画实现，避免重新引入 SVG dash 动画导致拐角和同步问题 |
